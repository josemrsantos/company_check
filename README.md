# company_check

```
usage: check.py [-h] [--list LIST] [--one ONE] [--queries QUERIES]

optional arguments:
  -h, --help         show this help message and exit
  --list LIST        File with campany names, line separated.
  --one ONE          One company name.
  --queries QUERIES  Queries to do.
```
